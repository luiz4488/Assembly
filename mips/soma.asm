.text #Como progama não vai pecisar da ram para executar os calculos não precisa de .data

	li $t0, 10
	li $t1, 15
	add $s0, $t0, $t1 # adiciona a soma de $t0 + $t1 no registrador $s0
	addi $s1, $t0, 30 # adiciona a soma de $t0 + 30 no registrador $s1
	
	